import { Path } from "./paths";
import { FileSystemType } from "./types";

const defaultPathFiles = { 
    files: [] as Path[],
    directories: [] as Path[]
} as FileSystemType


export {
    defaultPathFiles
}